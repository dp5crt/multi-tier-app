apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: app-production
  labels:
    app: web
    tier: frontend
spec:
  selector:
    app: web
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
  type: NodePort